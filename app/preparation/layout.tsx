import type { Metadata } from "next";

export const metadata: Metadata = { title: "賽前準備" };

export default function PreparationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
